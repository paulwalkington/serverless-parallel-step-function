export interface Input {
    Creation: boolean;
    Hitachi: boolean;
    V12: boolean;
    Etika: boolean;
}

export interface LambdaResponse {
    identifier: string;
    softSearchSuccess: Boolean;
}
